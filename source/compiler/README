Building Teyjus tjc:
	1. Build ocamake:
		In the util directory, build ocamake.ml:
		
		ocamlc unix.cma str.cma ocamake.ml -o ocamake.exe

		And then install it in your PATH.
	2. Compile:
		ocamake *.ml *.mli *.mly *.mll -opt -o tjc

Notes:
	Yes, this doesn't compile yet.
