# TODO - we only support Cygwin on Win32
if $(equal $(OSTYPE), Win32)
  OSTYPE = Cygwin
  GCC_FOUND = true
  GXX_FOUND = true
  export

open build/C
open build/OCaml

# TODO - remove following line to enable warn-error for OCaml
OCAMLFLAGS =

.SUBDIRS: .
